/**
 * Kannada.
 *
 * Empty on purpose. Every key falls through to English until a native speaker
 * fills it in — a machine-translated instruction on a screen where someone
 * copies a code onto fabric by hand is worse than plain English, because it
 * reads as authoritative and is not.
 *
 * Add keys in the same shape as en.ts; partial is fine and takes effect
 * immediately, key by key.
 */
export const kn = {} as const
